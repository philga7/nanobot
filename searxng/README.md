# SearXNG config for WrenVPS

Copy `settings.yml` to `~/.wrenvps/searxng/settings.yml` before starting the stack with the WrenVPS override. See [DOCKER.md](../DOCKER.md#wrenvps-searxng-config-on-host).
